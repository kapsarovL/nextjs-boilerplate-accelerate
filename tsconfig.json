{
  "compilerOptions": {
    // --- Type Checking ---
    "target": "ES2017", // Target ECMAScript version for emitted JavaScript. 'es2017' offers good compatibility.
    "lib": ["dom", "dom.iterable", "esnext"], // Standard libraries available during compilation.
    "allowJs": true, // Allow JavaScript files to be compiled alongside TypeScript files.
    "skipLibCheck": true, // Skip type checking of declaration files (*.d.ts). Speeds up compilation.
    "strict": true, // Enable all strict type-checking options (recommended for new projects).
    "noEmit": true,  // Do not emit JavaScript output files (Next.js handles transpilation).
    "esModuleInterop": true, // Enable compatibility with CommonJS modules using default imports.

    // --- Modules ---
    "module": "esnext", // Specify module code generation strategy. 'esnext' keeps modern module syntax.
    "moduleResolution": "bundler",  // How modules get resolved (aligned with modern bundlers like Webpack/Vite).
    "resolveJsonModule": true, // Allow importing JSON files as modules.
    "isolatedModules": true,  // Ensure each file can be safely transpiled without relying on other imports.

    // --- Emit ---
    // "noEmit": true is set above, so these are less relevant but good for reference.
    // "outDir": "./dist", // Output directory for emitted files (if noEmit is false).
    // "sourceMap": true, // Generate source map files for debugging.

    // --- JavaScript Support ---
    // "allowJs": true is set above.
    // "checkJs": false, // Type check JavaScript files (use JSDoc for types in JS).

    // --- Editor Integration ---
    // "rootDir": "./src", // Specifies the root directory of input files.

    // --- Project Structure ---
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next" // Integrate with Next.js's TypeScript plugin for enhanced features.
      }
    ],
    "paths": {
      // Path aliases for easier imports (configured during create-next-app).
      "@/*": ["./src/*"]
    }
     // Add any custom type roots if needed
    // "typeRoots": ["./node_modules/@types", "./src/types"]
  },
  // Files/directories to include in the compilation process.
  "include": [
    "next-env.d.ts", // Next.js environment type declarations.
    "**/*.ts", // Include all TypeScript files.
    "**/*.tsx", // Include types generated by Next.js.
    ".next/types/**/*.ts" // Include JS files if allowJs is true and you want them included explicitly
    // "**/*.js",
    // "**/*.jsx"
  ],
  "exclude": [
    "node_modules" // Exclude the node_modules directory.
    // Add other specific files or directories to exclude if necessary.
    // "dist",
    // "build"
  ]
}
